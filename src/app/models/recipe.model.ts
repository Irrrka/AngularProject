export interface RecipeModel {
        meal: string;
        ingredients: string;  
        prepMethod: string;
        foodImageURL: string;  
        category: string;   
        categoryImageURL: string;  
}